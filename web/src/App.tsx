import React, { useState } from 'react';
import './App.css';
import MainPage from './MainPage';

const App: React.FC = () => {
    return (
        <div className="container">
            <MainPage />
        </div>
    );
};

export default App;
